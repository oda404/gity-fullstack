query GetRepo(
    $name: String!
    $owner: String!
)
{
    getRepo(
        name: $owner
        owner: $owner
    )
    {
        repos{
            ...GenericRepo
        }

        error
    }
}