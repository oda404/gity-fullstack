query GetUserRepos(
    $owner: String!
    $count: Int!
    $start: Int = 0
)
{
    getUserRepos(
        owner: $owner
        count: $count
        start: $start
    )
    {
        ...GenericRepo
    }
}