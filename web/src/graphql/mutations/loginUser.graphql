mutation LoginUser(
  $usernameOrEmail: String!,
  $password: String!
)
{
  loginUser(userInput: {
    usernameOrEmail: $usernameOrEmail,
    password: $password
  })
  {
    errors{
      field,
      error
    }
    
    user{
      id
    }
  }
}