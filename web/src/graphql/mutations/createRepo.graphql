mutation CreateRepo(
    $name: String!
    $isPrivate: Boolean!
)
{
    createRepo(
        name: $name
        isPrivate: $isPrivate
    )
    {
        repos{
            ...GenericRepo
        }

        error
    }
}